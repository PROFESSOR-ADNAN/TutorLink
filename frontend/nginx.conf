# This Nginx config solves a key problem with React Router:
# When a user visits /tutors/123 directly (not via clicking a link),
# Nginx looks for a file at /tutors/123 — which doesn't exist.
# The "try_files $uri /index.html" line says:
# "Try the file, if not found, serve index.html instead"
# React Router then takes over and renders the correct page.

server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Gzip compression — reduces file sizes by ~70%
    gzip on;
    gzip_types text/html text/css application/javascript application/json;
    gzip_min_length 1000;

    # Cache static assets aggressively — they have hashed filenames from Vite
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # All routes fall back to index.html (React Router handles them)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";
    add_header Referrer-Policy "strict-origin-when-cross-origin";
}
